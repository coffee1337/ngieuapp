dependencies:
  flutter:
    sdk: flutter

  # State + DI
  flutter_riverpod: ^2.5.0
  riverpod_annotation: ^2.3.0

  # Networking
  dio: ^5.7.0
  pretty_dio_logger: ^1.4.0

  # Парсинг HTML (новости)
  html: ^0.15.0

  # Хранение
  drift: ^2.20.0          # SQLite для расписания + поиска кабинетов
  drift_flutter: ^0.2.0
  sqlite3_flutter_libs: ^0.5.0
  path_provider: ^2.1.0
  hive_flutter: ^1.1.0    # лёгкий кэш новостей (ключ → JSON)
  flutter_secure_storage: ^9.2.0  # токены/кука WebView

  # Роутинг
  go_router: ^14.2.0

  # WebView
  flutter_inappwebview: ^6.1.5

  # Утилиты
  freezed_annotation: ^2.4.1
  json_annotation: ^4.9.0
  intl: ^0.19.0
  cached_network_image: ^3.4.0

dev_dependencies:
  build_runner: ^2.4.0
  freezed: ^2.5.0
  json_serializable: ^6.8.0
  riverpod_generator: ^2.4.0
  drift_dev: ^2.20.0
  custom_lint: ^0.6.0
  riverpod_lint: ^2.3.0